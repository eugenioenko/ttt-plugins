# Markdown Preview

Preview markdown files with styled rendering.

## Commands

| Command | Description |
|---|---|
| `Preview: Markdown` | Render the current markdown buffer and open it in a new tab |

## Usage

Open a `.md` file, then run `Preview: Markdown` from the command palette (`Ctrl+P`).
